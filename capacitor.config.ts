import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DI06_TE01_plantilla',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
